<h2> <%= @message.title   %>   </h2>
<p>Posted by <%= @message.user.email  %> updated <%=time_ago_in_words(@message.updated_at)%> ago</p>
<p><%=   @message.description   %></p>


<h3>Comments:</h3>
<%=render @message.comments%>

<h3>Reply to Message:</h3>
<%=render 'comments/form' %>
<%=link_to 'back',root_path%>
<%if user_signed_in?%>
<%if @message.user_id == current_user.id%>
<%=link_to 'edit',edit_message_path%>

<%#=link_to 'Delete',message_path(@message), method: :delete,data: { confirm: "Are u sure?"}%>
<%end%>

<%end%>

